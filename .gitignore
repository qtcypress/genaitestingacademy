__pycache__/
*.pyc
_site/

# Generated by seo/build.py at build time; the sources are blog/posts/*.json
# and the page generators. Committing the output means two copies to keep in
# step and a diff on every build.
blog/*.html
